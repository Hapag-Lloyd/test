name: Call Docker reusable workflow

on:
  push:

jobs:
  docker:
    uses: ./.github/workflows/docker-callable.yml
    permissions:
      packages: write
      contents: read
    with:
      image-name: test
      push: true
